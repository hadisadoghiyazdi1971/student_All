# چک‌لیست استقرار دانشگاهی

## داده و مدل

- [ ] شناسه‌های `persons.id` و `articles.id` یکتا هستند.
- [ ] روابط نامعتبر مقاله و فرد حذف یا اصلاح شده‌اند.
- [ ] فایل‌های taxonomy توسط مالک داده تایید شده‌اند.
- [ ] مدل `qwen3:8b` در Ollama نصب است.
- [ ] مدل embedding پیش از شروع سرویس دانلود شده است.
- [ ] نمایه مقاله‌ها با `scripts/prebuild_embeddings.py` ساخته شده است.

## امنیت و حریم خصوصی

- [ ] برنامه فقط از شبکه مجاز دانشگاه قابل دسترسی است.
- [ ] HTTPS و SSO فعال هستند.
- [ ] متن نامه در log ثبت نمی‌شود.
- [ ] دسترسی به `data`، `cache` و `runtime` محدود شده است.
- [ ] سیاست نگهداری و حذف داده تعریف شده است.
- [ ] پشتیبان‌گیری و بازیابی آزمایش شده است.

## کیفیت

- [ ] مجموعه ارزیابی واقعی و ناشناس‌سازی‌شده آماده است.
- [ ] Precision@K و Recall@K ارجاع اندازه‌گیری شده‌اند.
- [ ] آستانه‌های keyword و field روی داده واقعی تنظیم شده‌اند.
- [ ] موارد confidence پایین به صف بررسی انسانی می‌روند.
- [ ] کیفیت فارسی و انگلیسی جداگانه ارزیابی شده است.
- [ ] خروجی پیش‌نویس از نظر عدم جعل اطلاعات بازبینی شده است.

## عملیات

- [ ] health check سرویس Streamlit و Ollama فعال است.
- [ ] مصرف GPU، RAM، دیسک و زمان پاسخ مانیتور می‌شود.
- [ ] cache و logها دارای محدودیت حجم هستند.
- [ ] برنامه با کاربر غیر root اجرا می‌شود.
- [ ] rollback نسخه قبلی آزمایش شده است.
